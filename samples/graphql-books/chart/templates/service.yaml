apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphql-books.fullname" . }}
  labels:
    {{- include "graphql-books.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "graphql-books.selectorLabels" . | nindent 4 }}
