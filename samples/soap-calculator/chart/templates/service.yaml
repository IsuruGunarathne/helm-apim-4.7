apiVersion: v1
kind: Service
metadata:
  name: {{ include "soap-calculator.fullname" . }}
  labels:
    {{- include "soap-calculator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "soap-calculator.selectorLabels" . | nindent 4 }}
