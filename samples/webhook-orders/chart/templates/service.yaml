apiVersion: v1
kind: Service
metadata:
  name: {{ include "webhook-orders.fullname" . }}
  labels:
    {{- include "webhook-orders.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "webhook-orders.selectorLabels" . | nindent 4 }}
